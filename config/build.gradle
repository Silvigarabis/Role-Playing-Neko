plugins {
    id 'java-library'
}

configurations {
    shadow
}

def configLoaderType = [
    "snakeYamlV2",
    "snakeYamlV1",
    "gson",
    "properties",
]

dependencies {
    api project()
    api project(":snakeYamlV2")
    api project(":gson")
    api project(":properties")
}

